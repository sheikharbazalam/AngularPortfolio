Como administrador da lista, sua autorização é solicitada para
a seguinte postagem na lista de discussão:

    Lista:   ${listname}
    De:      ${sender_email}
    Assunto: ${subject}

A mensagem está sendo retida pelo motivo:

${reasons}

Quando possível, visite seu painel para aprovar ou negar a
solicitação.
